apiVersion: security.istio.io/v1beta1
kind: PeerAuthentication
metadata:
  name: {{ include "external-secrets-chart.fullname" . }}-mtls
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "external-secrets-chart.labels" . | nindent 4 }}
spec:
  mtls:
    mode: STRICT
