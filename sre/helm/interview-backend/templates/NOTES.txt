Thank you for installing {{ .Chart.Name }}.

Your release is named {{ .Release.Name }}.

To learn more about the release, try:

  $ helm get all {{ .Release.Name }}

To get the application URL by running:

{{- if .Values.ingress.enabled }}
{{- range $host := .Values.ingress.hosts }}
  {{- range .paths }}
  http://{{ $host.host }}{{ .path }}
  {{- end }}
{{- end }}
{{- else }}
  $ kubectl port-forward svc/{{ include "interview-backend.fullname" . }} {{ .Values.service.port }}:{{ .Values.service.port }}
  Then access the application at http://localhost:{{ .Values.service.port }}
{{- end }}